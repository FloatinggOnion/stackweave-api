[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers
markers =
    unit: Unit tests (fast, no external services)
    integration: Integration tests (may require solver, FastAPI)
    slow: Slow tests (>1 second)
minversion = 7.0
